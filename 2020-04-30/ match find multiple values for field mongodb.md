###  match find multiple values for field mongodb


[$in — MongoDB Manual](https://docs.mongodb.com/manual/reference/operator/query/in "$in — MongoDB Manual")


 

```js
db.inventory.find( { qty: { $in: [ 5, 15 ] } } )

```
